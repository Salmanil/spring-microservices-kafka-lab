{
  "index_patterns": ["employee-events*"],
  "template": {
    "mappings": {
      "properties": {
        "eventTimestamp": {
          "type": "date"
        },
        "kafka_timestamp": {
          "type": "date",
          "format": "epoch_millis"
        }
      }
    }
  }
}
